<!DOCTYPE html>
<html class="loading" lang="en" data-textdirection="ltr">

<head>
  <%- include('_layouts/head') %>
    <title>User Profile | <%= title %>
    </title>

    <link rel="stylesheet" type="text/css" href="/app-assets/css/pages/page-users.css">
    <link rel="stylesheet" type="text/css" href="/app-assets/vendors/data-tables/css/jquery.dataTables.min.css">
    <link rel="stylesheet" type="text/css" href="/app-assets/vendors/data-tables/css/select.dataTables.min.css">
    <link rel="stylesheet" type="text/css" href="/app-assets/css/pages/data-tables.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <style>
      /* table tr td, th { text-align: center; vertical-align: middle; } */

      .switch label input[type=checkbox]:not(:checked)+.lever {
        background-color: #f3374a;
      }

      .switch label input[type=checkbox]:not(:checked)+.lever::before {
        background-color: #ffaba5;
      }

      .switch label input[type=checkbox]:checked+.lever {
        background-color: #31db37;
      }

      .switch label input[type=checkbox]:checked+.lever::before {
        background-color: #4CAF50;
      }

      .chip {
        border-radius: 30px;
        padding: 6px 16px;
        font-weight: 600;
        font-size: 13px;
      }

      code {
        background: #f5f5f5;
        padding: 4px 10px;
        border-radius: 8px;
        font-size: 13px;
        font-family: 'Courier New', monospace;
      }

      .new.badge.red {
        background-color: #e57373 !important;
        color: white;
      }

      /* PERFECT SCROLL + NO WHITE SPACE */
      .dataTables_wrapper .dataTables_scrollBody {
        -webkit-overflow-scrolling: touch;
      }

      #page-length-option_wrapper {
        overflow-x: auto;
        margin-bottom: 20px;
      }

      #page-length-option {
        min-width: 800px;
        width: 100%;
      }

      #page-length-option th,
      #page-length-option td {
        white-space: nowrap;
      }

      /* WHITE SPACE KHATAM */
      body,
      html {
        overflow-x: hidden !important;
      }

      .container {
        width: 100% !important;
        max-width: 100% !important;
        padding: 0 15px;
      }

      .row {
        margin: 0;
      }

      .col {
        padding: 0;
      }

      @media (max-width: 768px) {
        .mobile-center {
          text-align: center !important;
        }

        .mobile-block {
          display: block !important;
          width: 100% !important;
          margin: 10px 0 !important;
        }

        .mobile-mt {
          margin-top: 20px !important;
        }
      }
    </style>
</head>

<body
  class="vertical-layout page-header-light vertical-menu-collapsible vertical-dark-menu preload-transitions 2-columns">
  <%- include('_layouts/sidenavbar') %>

    <div id="main">
      <div class="row">
        <div id="breadcrumbs-wrapper" style="background-color: white;">
          <div class="container">
            <div class="row">
              <div class="col s12 m6 l6">
                <h5 class="breadcrumbs-title"><span>User Profile</span></h5>
              </div>
              <div class="col s12 m6 l6 right-align-md">
                <a class="btn gradient-45deg-purple-deep-orange modal-trigger" href="#notifyModal">Send Notification</a>
              </div>
            </div>
          </div>
        </div>

        <%- include('messages', { messages: messages }) %>

          <div class="col s12">
            <div class="container">
              <div class="section users-view">

                <!-- USER INFO CARD -->
                <div class="card-panel">
                  <div class="row">
                    <div class="col s12 m7">
                      <div class="display-flex media">
                        <img src="<%= process.env.IMAGE_URL + user.qrCode %>" alt="QR Code" height="80" width="80"
                          style="border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.2);">
                        <div class="media-body" style="margin-left: 15px;">
                          <h6 style="margin: 8px 0; font-size: 1.6rem; font-weight: 600;">
                            <%= user.name || 'Guest User' %>
                          </h6>
                          <p style="margin: 6px 0;"><strong>Mobile:</strong>
                            <%= user.mobileNumber %>
                          </p>
                          <p style="margin: 6px 0;"><strong>Email:</strong>
                            <%= user.email || '-' %>
                          </p>
                          <p style="margin: 6px 0;"><strong>Registered:</strong>
                            <%= new Date(user.createdAt).toLocaleDateString('en-GB') %>
                          </p>
                        </div>
                      </div>
                    </div>

                    <!-- Desktop Buttons -->
                    <div class="col s12 m5 mobile-center hide-on-small-only" style="text-align: right;">
                      <div class="switch" style="margin-top: 20px;">
                        <label>
                          <span style="color: red; font-weight: bold;">Blocked</span>
                          <input type="checkbox" <%=user.isActive ? 'checked' : '' %> onchange="toggleUserStatus('<%=
                            user._id %>', this.checked)">
                            <span class="lever"></span>
                            <span style="color: green; font-weight: bold;">Active</span>
                        </label>
                      </div>
                      <div style="margin-top: 20px;">
                        <a href="/admin/user/user-delete/<%= user._id %>" class="btn-small red waves-effect waves-light"
                          onclick="return confirm('Are you sure you want to delete this user permanently?');">
                          <i class="material-icons left">delete</i>Delete
                        </a>
                      </div>
                    </div>

                    <!-- Mobile Buttons -->
                    <div class="col s12 show-on-small hide-on-med-and-up center mobile-mt">
                      <div class="switch">
                        <label>
                          <span style="color: red; font-weight: bold;">Blocked</span>
                          <input type="checkbox" <%=user.isActive ? 'checked' : '' %> onchange="toggleUserStatus('<%=
                            user._id %>', this.checked)">
                            <span class="lever"></span>
                            <span style="color: green; font-weight: bold;">Active</span>
                        </label>
                      </div>
                      <a href="/admin/user/user-delete/<%= user._id %>"
                        class="btn-small red waves-effect waves-light mobile-block"
                        onclick="return confirm('Are you sure you want to delete this user permanently?');">
                        <i class="material-icons left">delete</i>Delete User
                      </a>
                    </div>
                  </div>
                </div>

                <!-- USER STATS -->
                <div class="card">
                  <div class="card-content">
                    <h5>User Statistics</h5>
                    <table class="striped">
                      <tbody>
                        <tr>
                          <th>Total Points Earned</th>
                          <td><strong>
                              <%= user.totalPoints || 0 %> pts
                            </strong></td>
                        </tr>
                        <tr>
                          <th>Points Redeemed</th>
                          <td><strong class="red-text">
                              <%= user.pointsRedeemed || 0 %> pts
                            </strong></td>
                        </tr>
                        <tr>
                          <th>Current Balance</th>
                          <td><strong class="green-text">
                              <%= user.currentPoints || 0 %> pts
                            </strong></td>
                        </tr>
                        <tr>
                          <th>Total Orders</th>
                          <td><strong>
                              <%= user.totalOrders || 0 %>
                            </strong></td>
                        </tr>
                      </tbody>
                    </table>
                  </div>
                </div>

                <!-- POINT REDEMPTION HISTORY — DATE & TIME BILKUL ORDER DETAILS JAISE -->
                <div class="card">
                  <div class="card-content">
                    <h5>Point Redemption History</h5>
                    <div class="section section-data-tables">
                      <div class="row">
                        <div class="col s12">
                          <table id="page-length-option" class="display" style="width:100%">
                            <thead>
                              <tr>
                                <th>Sr</th>
                                <th>Date & Time</th>
                                <th>Vendor</th>
                                <th>Branch</th>
                                <th>Item</th>
                                <th>Points Used</th>
                                <th>Discount (BHD)</th>
                                <th>Order ID</th>
                              </tr>
                            </thead>
                            <tbody>
                              <% if(redemptions && redemptions.length> 0) { %>
                                <% redemptions.forEach((r, i)=> { %>
                                  <tr>
                                    <td>
                                      <%= i + 1 %>
                                    </td>
                                    <td style="white-space: nowrap;">
                                      <% const d=new Date(r.date); const day=String(d.getDate()).padStart(2, '0' );
                                        const month=String(d.getMonth() + 1).padStart(2, '0' ); const
                                        year=d.getFullYear(); const hours=String(d.getHours()).padStart(2, '0' ); const
                                        minutes=String(d.getMinutes()).padStart(2, '0' ); %>
                                        <%= `${day}-${month}-${year} ${hours}:${minutes}` %>
                                    </td>
                                    <td><span class="chip teal lighten-4">
                                        <%= r.vendorName %>
                                      </span></td>
                                    <td><span class="chip amber lighten-4"><strong>
                                          <%= r.branchName || '-' %>
                                        </strong></span></td>
                                    <td>
                                      <%= r.productName || 'General Discount' %>
                                    </td>
                                    <td><span class="new badge white-text" style="background-color: #f44336 ;" data-badge-caption="pts">
                                        <%= r.pointsRedeemed %>
                                      </span></td>
                                    <td><strong class="green-text">
                                        <%= r.discountGiven.toFixed(3) %>
                                      </strong></td>
                                    <td><code>#<%= r.transactionId.toString().slice(-8) %></code></td>
                                  </tr>
                                  <% }) %>
                                    <% } else { %>
                                      <tr>
                                        <td colspan="8" class="center grey-text">No redemption history</td>
                                      </tr>
                                      <% } %>
                            </tbody>
                          </table>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>

              </div>
            </div>
          </div>
      </div>
    </div>

    <!-- Notify Modal -->
    <div id="notifyModal" class="modal">
      <div class="modal-content">
        <h5>Send Notification</h5>
        <form method="POST" action="/admin/user/notify-single">
          <input type="hidden" name="userId" value="<%= user._id %>">
          <div class="input-field"><input type="text" name="title" required><label>Title</label></div>
          <div class="input-field"><textarea name="body" class="materialize-textarea"
              required></textarea><label>Message</label></div>
          <button type="submit" class="btn purple">Send</button>
        </form>
      </div>
    </div>

    <%- include('_layouts/commonJs') %>

      <script src="/app-assets/vendors/data-tables/js/jquery.dataTables.min.js"></script>
      <script src="/app-assets/js/scripts/data-tables.js"></script>

      <script>
        $(document).ready(function () {
          $('.modal').modal();
        });

        function toggleUserStatus(id, status) {
          if (confirm(`Are you sure you want to ${status ? 'activate' : 'block'} this user?`)) {
            location.href = `/admin/user/change-status/${id}/${status ? 1 : 0}`;
          } else {
            setTimeout(() => location.reload(), 100);
          }
        }
      </script>
</body>

</html>